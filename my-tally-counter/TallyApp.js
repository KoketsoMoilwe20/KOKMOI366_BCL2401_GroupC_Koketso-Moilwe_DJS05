const initialState = {
    count: 0
}

const getState = () => {
    return initialState;
}

//Defining Action Types
{ type: 'increment'}
{ type: 'decrement'}

const counter = (state = 0, action) => {
    switch 
}

